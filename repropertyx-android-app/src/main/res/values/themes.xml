<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RePropertyX" parent="Theme.Material3.DayNight">
        <!-- Use default Material3 color scheme - don't override colors -->
        <!-- This ensures consistency with Compose MaterialTheme defaults -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
    </style>
</resources>
